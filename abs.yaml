# We aren't strictly tied to ventura, but calls to gst-inspect-1.0 --version hang on specific workers.
# It seems to be due to running in a headless environment. 
extra_labels_for_os:
  osx-64: [ventura]
  osx-arm64: [ventura]
